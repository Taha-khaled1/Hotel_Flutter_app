import 'package:flutter/material.dart';

class HotelDetelisScreen extends StatelessWidget {
  const HotelDetelisScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(),
    );
  }
}
